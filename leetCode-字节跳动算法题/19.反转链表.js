var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
  return prev
}

function ListNode (val) {
  this.val = val
  this.next = null
}

console.log(reverseList(
  [1, 2, 3, 4, 5]))

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
  return prev
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
  return prev
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
  return prev
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {
      curr.next = prev
    }
    prev = curr
    curr = cnext
  }
  return prev
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    if (prev === null) {
      curr.next = null
    } else {

    }
  }
}

var reverseList = function (head) {
  let prev = null
  let curr = head
  while (curr !== null) {
    let cnext = curr.next
    curr.next = prev
    prev = curr
    curr = cnext
  }
  return prev
}