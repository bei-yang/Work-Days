/* 
  createElement

  数组的childrenLength

  还有一个参数 default props

  有默认的props取props

  cloneElement

  克隆并且返回一个新的element

  原先就是创造了一个，现在是克隆

  遍历config，默认的props

  children就完全一样了

  component base class

  函数组件和类组件

  shouldComponentUpdate

  updateContainer 做更新操作

  0
*/